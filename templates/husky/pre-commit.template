#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "Running pre-commit checks..."

# Run all checks via package.json scripts (turbo)
# fail fast on first error
bun run lint || exit 1
bun run typecheck || exit 1

echo "All checks passed!"